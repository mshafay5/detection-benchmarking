# Benchmark Notes

The public-facing project documentation is now in `README.md`.

Local datasets, checkpoints, logs, and benchmark outputs are intentionally ignored by Git so this repository stays focused on the reusable benchmarking code.
